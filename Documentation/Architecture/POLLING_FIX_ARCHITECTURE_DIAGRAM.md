# E2E Polling Fix - System Architecture Diagram

## Complete Data Flow: User → Frontend → ComfyUI → Results

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           USER INTERFACE LAYER                              │
│                           (React Components)                                │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ User clicks "Generate Video"
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Component: GenerationControls.tsx / TimelineEditor.tsx                     │
│  - Displays UI buttons, progress indicators                                 │
│  - Shows status: queued → running → complete                                │
│  - Renders error messages                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Calls generateTimelineVideos()
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  React Hook: useProjectData (utils/hooks.ts)                                │
│  - Manages application state                                                │
│  - Orchestrates multi-step generation                                       │
│  - Persists data to IndexedDB                                               │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Delegates to service layer
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SERVICE LAYER                                       │
│                   (services/comfyUIService.ts)                              │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────┐          │
│  │ generateTimelineVideos()                                      │          │
│  │ - Batch processes all shots in timeline                       │          │
│  │ - Calls generateVideoFromShot() for each shot                 │          │
│  └──────────────────────────────────────────────────────────────┘          │
│                           │                                                 │
│                           │ For each shot                                   │
│                           ▼                                                 │
│  ┌──────────────────────────────────────────────────────────────┐          │
│  │ generateVideoFromShot()                                       │          │
│  │ - Builds prompts with enhancers                               │          │
│  │ - Validates keyframe image exists                             │          │
│  │ - Calls queueComfyUIPrompt()                                  │          │
│  │ - Calls trackPromptExecution()                                │          │
│  └──────────────────────────────────────────────────────────────┘          │
│           │                                 │                               │
│           │ Step 1: Queue                   │ Step 2: Track                 │
│           ▼                                 ▼                               │
│  ┌──────────────────┐            ┌──────────────────────┐                  │
│  │queueComfyUIPrompt│            │trackPromptExecution() │                  │
│  │                  │            │                       │                  │
│  │ - Uploads image  │            │ - Opens WebSocket     │                  │
│  │ - Injects prompts│            │ - Receives updates    │                  │
│  │ - POSTs workflow │            │ - Calls onProgress()  │                  │
│  │ - Returns promptId│            │ - Fetches outputs     │                  │
│  └──────────────────┘            └──────────────────────┘                  │
│           │                                 │                               │
└───────────┼─────────────────────────────────┼───────────────────────────────┘
            │                                 │
            │ HTTP POST /prompt               │ WebSocket /ws
            ▼                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       COMFYUI BACKEND                                        │
│                   (Python HTTP Server + WebSocket)                          │
│                        Port 8188                                             │
│                                                                              │
│  ┌────────────────────┐  ┌────────────────────┐  ┌────────────────────┐   │
│  │  HTTP REST API     │  │   WebSocket API    │  │   Queue System     │   │
│  │                    │  │                    │  │                    │   │
│  │ POST /prompt       │  │ /ws?clientId=...   │  │ queue_running: [   │   │
│  │ → Queue workflow   │  │ → Real-time events │  │   [timestamp, id]  │   │
│  │                    │  │                    │  │ ]                  │   │
│  │ GET /queue         │  │ Events:            │  │                    │   │
│  │ → Running/pending  │  │ - execution_start  │  │ queue_pending: [   │   │
│  │                    │  │ - executing        │  │   [timestamp, id]  │   │
│  │ GET /history/{id}  │  │ - progress         │  │ ]                  │   │
│  │ → Completed results│  │ - executed         │  │                    │   │
│  │                    │  │ - execution_error  │  │                    │   │
│  └────────────────────┘  └────────────────────┘  └────────────────────┘   │
│                                                                              │
│  Workflow Execution:                                                        │
│  1. Load workflow JSON                                                      │
│  2. Inject prompts via mappings                                             │
│  3. Execute nodes sequentially                                              │
│  4. Save outputs to disk                                                    │
│  5. Return results via WebSocket                                            │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Results flow back up
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND RECEIVES RESULTS                                 │
│                                                                              │
│  1. trackPromptExecution() receives WebSocket event:                        │
│     { type: 'executed', data: { output: { videos: [...] } } }              │
│                                                                              │
│  2. Fetches video file via HTTP:                                            │
│     GET /view?filename=scene-001.mp4&subfolder=video&type=output            │
│                                                                              │
│  3. Converts to data URL:                                                   │
│     data:video/mp4;base64,AAAAHGZ0eXBpc29t...                               │
│                                                                              │
│  4. Calls onProgress() with final output:                                   │
│     { status: 'complete', final_output: { type: 'video', data: '...' } }   │
│                                                                              │
│  5. UI updates to show video player                                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Status Polling Flow (E2E Fix Focus)

### **BEFORE FIX** ❌

```
PowerShell E2E Script (Testing Backend):
┌─────────────────────────────────────────┐
│ Check-PromptStatus function             │
│                                          │
│ 1. Check /history/{promptId}             │
│    └─ If found: return 'completed'       │
│    └─ If not found: return 'pending' ❌  │
│                                          │
│ Problem: Running prompts are NOT in      │
│ history yet! They're in the queue!       │
└─────────────────────────────────────────┘
         │
         │ Result: Status shows 'pending' for 142+ seconds
         │ while video is actually generating
         ▼
    ❌ FALSE STATUS ❌
```

### **AFTER FIX** ✅

```
PowerShell E2E Script (Testing Backend):
┌─────────────────────────────────────────┐
│ Check-PromptStatus function             │
│                                          │
│ 1. Check /queue ✅ (NEW!)                │
│    └─ If in queue_running:               │
│       return 'running' ✅                │
│    └─ If in queue_pending:               │
│       return 'queued' ✅                 │
│                                          │
│ 2. Check /history/{promptId}             │
│    └─ If found with outputs:             │
│       return 'completed' ✅              │
│    └─ If found with errors:              │
│       return 'error' ✅                  │
│                                          │
│ 3. Handle race conditions                │
│    └─ If not in queue AND not in history:│
│       return 'pending' (truly pending) ✅│
└─────────────────────────────────────────┘
         │
         │ Result: Status accurately reflects
         │ actual execution state
         ▼
    ✅ ACCURATE STATUS ✅
```

## React Frontend Status Tracking (Parallel Pattern)

The frontend uses **dual tracking** for reliability:

```
┌────────────────────────────────────────────────────────────────┐
│              PRIMARY: WebSocket Real-Time Updates              │
│                                                                 │
│  trackPromptExecution()                                         │
│  ├─ WebSocket connection to ComfyUI                            │
│  ├─ Receives events: execution_start, executing, progress      │
│  ├─ Calls onProgress({ status: 'running', message: '...' })   │
│  └─ On 'executed': Fetches outputs and calls                   │
│     onProgress({ status: 'complete', final_output: {...} })    │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ Parallel safety net
                              ▼
┌────────────────────────────────────────────────────────────────┐
│              FALLBACK: HTTP Polling (Every 2 seconds)          │
│                                                                 │
│  setInterval(() => {                                            │
│    const queueInfo = await getQueueInfo(url);                  │
│    if (queueInfo.queue_running === 0 && outputData) {          │
│      // Generation complete, resolve promise                   │
│      resolve(outputData);                                       │
│    }                                                            │
│  }, 2000);                                                      │
└────────────────────────────────────────────────────────────────┘

Why Both?
- WebSocket: Low latency, real-time updates
- Polling: Ensures completion even if WebSocket disconnects
- Matches E2E test pattern: Both methods validated
```

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────┐
│               Error Detection Points                          │
└──────────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Pre-Flight   │  │ Queue/Track  │  │ Post-Process │
│ Validation   │  │ Errors       │  │ Errors       │
│              │  │              │  │              │
│ - Server     │  │ - Network    │  │ - Missing    │
│   unreachable│  │   timeout    │  │   outputs    │
│ - Invalid    │  │ - WebSocket  │  │ - File fetch │
│   workflow   │  │   disconnect │  │   failed     │
│ - Missing    │  │ - Execution  │  │              │
│   mappings   │  │   error      │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
                         │ All errors include context:
                         │ Scene ID, Shot ID, Prompt ID
                         ▼
        ┌────────────────────────────────┐
        │ onProgress({                    │
        │   status: 'error',              │
        │   message: 'Scene scene-001 ... │
        │ })                              │
        └────────────────────────────────┘
                         │
                         │ Propagates to UI
                         ▼
        ┌────────────────────────────────┐
        │ User sees:                      │
        │ "Error: Scene scene-001 failed: │
        │  Workflow validation failed..."  │
        └────────────────────────────────┘
```

## Queue Position Tracking

```
User submits 3 video generation requests:
┌────────────────────────────────────────┐
│ ComfyUI Queue State                     │
│                                         │
│ queue_running: [                        │
│   [1732071655000, "prompt-abc-001"]     │  ← Shot 1 (running)
│ ]                                       │
│                                         │
│ queue_pending: [                        │
│   [1732071656000, "prompt-abc-002"],    │  ← Shot 2 (position 1)
│   [1732071657000, "prompt-abc-003"]     │  ← Shot 3 (position 2)
│ ]                                       │
└────────────────────────────────────────┘
                   │
                   │ WebSocket status event
                   ▼
        ┌──────────────────────┐
        │ { type: 'status',    │
        │   data: {            │
        │     queue_remaining: 2│
        │   }                  │
        │ }                    │
        └──────────────────────┘
                   │
                   │ Handled by trackPromptExecution()
                   ▼
        ┌──────────────────────┐
        │ onProgress({         │
        │   status: 'queued',  │
        │   message: 'In queue │
        │     Position: 2',    │
        │   queue_position: 2  │
        │ })                   │
        └──────────────────────┘
                   │
                   │ Propagates to UI
                   ▼
        ┌──────────────────────┐
        │ User sees:           │
        │ 🟡 Queued...         │
        │    Position: 2       │
        └──────────────────────┘
```

## Data Persistence Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Generation Results                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Final output received
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ services/comfyUIService.ts                                   │
│ - Converts video file to base64 data URL                    │
│ - Returns: { videoPath: 'data:video/mp4;base64,...' }      │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Passed to hook
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ utils/hooks.ts → useProjectData                              │
│ - Stores result in state                                    │
│ - Triggers usePersistentState auto-sync                     │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Auto-persisted
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ utils/database.ts → IndexedDB                                │
│ - Saves video data URL                                      │
│ - Persists across browser sessions                          │
│ - Enables offline access                                    │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ On page reload
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Auto-restore from IndexedDB                                  │
│ - Videos immediately available                               │
│ - No re-generation needed                                   │
└─────────────────────────────────────────────────────────────┘
```

## E2E Test Validation Flow

```
┌────────────────────────────────────────────────────────────┐
│ PowerShell E2E Script (Backend Validation)                 │
│ scripts/run-comfyui-e2e.ps1                                │
└────────────────────────────────────────────────────────────┘
                         │
                         │ Calls for each scene
                         ▼
┌────────────────────────────────────────────────────────────┐
│ scripts/generate-scene-videos-wan2.ps1                      │
│ 1. Queue prompt via HTTP POST /prompt                       │
│ 2. Poll status with Check-PromptStatus (FIXED!)            │
│ 3. Wait for completion                                      │
│ 4. Copy video files to output directory                     │
│ 5. Log results to run-summary.txt                           │
└────────────────────────────────────────────────────────────┘
                         │
                         │ Validates same APIs used by frontend
                         ▼
┌────────────────────────────────────────────────────────────┐
│ Test Results (logs/20251119-205415/run-summary.txt)        │
│                                                             │
│ [20:54:17] [scene-001] Wan2 status: running (0s)          │
│ [20:54:18] [scene-001] Wan2 status: running (1s)          │
│ ...                                                         │
│ [20:57:55] [scene-001] Wan2 video generation succeeded:    │
│            scene-001.mp4 (217.6s, 0.33 MB)                 │
│                                                             │
│ ✅ Status showed 'running' throughout (not 'pending')      │
│ ✅ Completion detected correctly                            │
│ ✅ Video copied successfully                                │
└────────────────────────────────────────────────────────────┘
                         │
                         │ Proves frontend pattern works
                         ▼
┌────────────────────────────────────────────────────────────┐
│ Frontend Confidence: If E2E tests pass, React UI will work │
│ - Same APIs (queue, history, WebSocket)                    │
│ - Same status detection logic                              │
│ - Same error handling patterns                             │
│ → Production-ready ✅                                       │
└────────────────────────────────────────────────────────────┘
```

## Summary: How the Fix Translates

| Layer | Before Fix ❌ | After Fix ✅ | Impact |
|-------|--------------|-------------|---------|
| **PowerShell E2E** | Only checked `/history`, showed "pending" for running prompts | Checks `/queue` first, accurate status | Validates frontend patterns work under load |
| **React Frontend** | Already used queue info, but no validation | Same code, now validated by E2E tests | Confidence in production reliability |
| **User Experience** | Potentially stalled "Queued..." messages | Accurate "Generating... 45%" progress | Better feedback, no confusion |
| **Error Handling** | Silent failures possible | All errors logged with context | Easier debugging for users/devs |
| **Queue Awareness** | Basic queue position display | Validated queue tracking | Accurate queue position shown |

**Bottom Line**: The E2E fix doesn't change frontend code (already correct), but **proves it works reliably** and **catches edge cases** that could cause problems in production.
